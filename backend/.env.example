PORT=3000
DATABASE_URL=postgres://cards:cards@localhost:5432/cards_collection
JWT_SECRET=cambia-este-secreto-en-produccion
JWT_EXPIRES_IN=30d
CORS_ORIGIN=http://localhost:4200
NODE_ENV=development
# Vacío => automático según el protocolo de la petición. true/false para forzarlo.
COOKIE_SECURE=

# Opcional: crea este usuario como admin al arrancar si no existe ya.
# ADMIN_USER=admin
# ADMIN_PASSWORD=admin1234
